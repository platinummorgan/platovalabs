import { UPDATES } from '@/data/updates';
import Link from 'next/link';

# Updates

<div className="flex items-center justify-between">
  <h1 className="text-3xl font-bold">Updates</h1>
  <Link href="/" className="text-sm text-cyan-400">Back to Home</Link>
</div>

<p className="mt-2 text-sm text-neutral-400">A chronological feed of releases and important fixes.</p>

## Rebalance {#rebalance}

{UPDATES.filter(u => u.category === 'rebalance').map(u => (
  <div key={u.id} className="mt-4">
    <p>
      <strong>{u.version} — {u.title}</strong> — <time dateTime={u.date}>{new Date(u.date).toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</time>
    </p>
    <p className="text-sm text-neutral-300">{u.description}</p>
  </div>
))}

<div className="mt-8">
  <Link href="#mobile-apps" className="text-sm text-neutral-300">← Mobile Apps</Link>
</div>

title: "Updates — Platova Labs"
---

# Updates

## October 2025

- Rebalance 1.0.2 — small performance and stability improvements.
- QRKit tweak — adjusted default sizes for better scanning compatibility.
- PDFTrim bugfix — fixed an edge case when trimming large documents.

## September 2025

- Rebalance 1.0.1 — initial public release.
- QRKit.app — small UX polish and share link fix.
- PDFTrim — improved compression heuristics.
export const metadata = {
  title: "Updates — Platova Labs",
};

# Updates

## October 2025

- Rebalance 1.0.2 minor improvements and stability fixes
- QRKit small fix for clipboard copy
- PDFTrim bugfix for large files

## September 2025

- Rebalance initial release
- QRKit performance improvements
